server:
  port: 8080

spring:
  redis:
    host: localhost
    port: 6379
    password: 123456
    timeout: 5s
    lettuce:
      pool:
        min-idle: 0
        max-active: 20
        max-idle: 8
        max-wait: -1ms

